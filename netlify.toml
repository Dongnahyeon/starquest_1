[build]
command = "node scripts/build-web.js && pnpm build"
publish = "dist/web"

[build.environment]
NODE_VERSION = "20"
NODE_ENV = "production"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
